- hosts: localhost
  connection: local
  gather_facts: False
  tasks:
    - name: Import VPC setup Variables
      include_vars: vars/bastion_setup

    - name: Import VPC setup Variables
      include_vars: vars/output_vars
      